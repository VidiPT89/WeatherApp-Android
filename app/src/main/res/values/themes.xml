<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat/DayNight parent is required once MainActivity extends AppCompatActivity
         (needed for the per-app language backport below Android 13); Compose's setContent {}
         still owns all UI, this theme is only used for the window background/decor before
         Compose takes over. -->
    <style name="Theme.WeatherApp" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
