<?xml version="1.0" encoding="utf-8"?>
<!--
    Reasonable min size for a small/medium at-a-glance weather widget (roughly 2x1 cells).
    updatePeriodMillis is 0: this widget is app-driven only (see WeatherWidgetRepository), it
    never fetches on its own, so there is no periodic system refresh to configure.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="80dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@mipmap/ic_launcher"
    android:description="@string/widget_description" />
