<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The backend runs over plain HTTP on the dev machine. The emulator reaches the host
         machine's localhost via the special alias 10.0.2.2, so cleartext traffic is allowed
         only for that address (plus localhost/127.0.0.1 for physical-device-over-adb-reverse
         setups). Production builds pointed at a real HTTPS backend would not need this. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
